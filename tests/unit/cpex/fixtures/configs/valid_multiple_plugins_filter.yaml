plugins:
  # Self-contained Deny List Plugin
  - name: "DenyListPlugin"
    kind: "plugins.deny_filter.DenyListPlugin"
    description: "A plugin that implements a deny list filter."
    version: "0.1"
    author: "ContextForge Team"
    hooks: ["prompt_pre_fetch"]
    tags: ["plugin", "filter", "denylist", "pre-post"]
    mode: "sequential"  # sequential: can block and modify
    priority: 100
    conditions:
      # Apply to specific tools/servers
      - prompts: ["test_prompt"]
        server_ids: []  # Apply to all servers
        tenant_ids: []  # Apply to all tenants
    config:
      words:
        - innovative
        - groundbreaking
        - revolutionary
  # Self-contained Search Replace Plugin
  - name: "ReplaceBadWordsPlugin"
    kind: "plugins.search_replace.SearchReplacePlugin"
    description: "A plugin for finding and replacing words."
    version: "0.1"
    author: "ContextForge Team"
    hooks: ["prompt_pre_fetch", "prompt_post_fetch"]
    tags: ["plugin", "transformer", "regex", "search-and-replace", "pre-post"]
    mode: "transform"  # transform: can modify payloads, cannot block
    priority: 150
    conditions:
      # Apply to specific tools/servers
      - prompts: ["test_prompt"]
        server_ids: []  # Apply to all servers
        tenant_ids: []  # Apply to all tenants
    config:
      words:
        - search: crap
          replace: crud
        - search: crud
          replace: yikes
  - name: "ResourceFilterExample"
    kind: "plugins.resource_filter.ResourceFilterPlugin"
    description: "Demonstrates resource pre/post fetch hooks for filtering and validation"
    version: "1.0.0"
    author: "ContextForge Team"
    hooks: ["resource_pre_fetch", "resource_post_fetch"]
    tags: ["resource", "filter", "security", "example"]
    mode: "sequential"  # Block resources that violate rules
    priority: 200
    conditions: []  # Apply to all resources
    config:
      # Maximum content size in bytes (1MB)
      max_content_size: 1048576
      # Allowed protocols (removing file for testing)
      allowed_protocols:
        - test
        - time
        - timezone
        - http
        - https
      # Blocked domains (examples)
      blocked_domains:
        - malicious.example.com
        - untrusted-site.net
      # Content filters to redact sensitive data
      content_filters:
        - pattern: "password\\s*[:=]\\s*\\S+"
          replacement: "password: [REDACTED]"
        - pattern: "api[_-]?key\\s*[:=]\\s*\\S+"
          replacement: "api_key: [REDACTED]"
        - pattern: "secret\\s*[:=]\\s*\\S+"
          replacement: "secret: [REDACTED]"


# Plugin directories to scan
plugin_dirs:
  - "tests/unit/cpex/fixtures"

# Global plugin settings
plugin_settings:
  parallel_execution_within_band: true
  plugin_timeout: 30
  fail_on_plugin_error: false
  enable_plugin_api: true
  plugin_health_check_interval: 60
